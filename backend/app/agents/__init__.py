"""Agent runtime: contracts, tool loop, routing, governance, memory, intent fusion.

Business services call this package. They do not talk to a vendor SDK themselves.
"""

"""Agent runtime package. Import submodules directly to avoid pulling the tool loop at startup."""
